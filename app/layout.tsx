import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Rainbow Foods & Bakery - Fresh Baked Goods Daily',
  description: 'Rainbow Foods & Bakery offers fresh baked goods, artisan breads, pastries, and custom cakes. Visit us for the finest quality bakery products.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
